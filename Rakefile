ENV["EVENTS_ENV"] ||= "development"

# require_relative './apps/models/data_importer'
# require_relative './apps/models/api_adapter'
require_relative './config/environment'
require 'sinatra/activerecord/rake'

# Type `rake -T` on your command line to see the available rake tasks.

task :console do
  Pry.start
end
